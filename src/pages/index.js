// import { lazy } from "react";
// export const Products = lazy(() => import("./Products/Products"))

export { default as Products } from "./Products/Products" // we don't need lazy load since we don't have more pages